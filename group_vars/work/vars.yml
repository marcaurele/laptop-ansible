---

aws_config : "{{ vault_aws_config }}"

certificates_extra: "{{ vault_certificates_extra }}"

company: "{{ vault_company }}"

email_work: "{{ vault_email }}"

git_work: "{{ vault_git_work }}"

#grub_parameter: "resume=UUID=468a7c6e-054d-49c2-b44d-39e88f3121d6"

nodejs:
  version: 14.x

# packages_extra:
#  - gcalcli

# packages_absent_extra:
#   - awscli

pip_extra_index_url: "{{ vault_pip_extra_index_url }}"

private_node: false

username: mbrothie
