---

- name: add repository GPG key for HashiCorp
  become: true
  ansible.builtin.get_url:
    url: https://apt.releases.hashicorp.com/gpg
    dest: /etc/apt/trusted.gpg.d/hashicorp.asc

- name: add reposiroty for HashiCorp on Debian
  become: true
  apt_repository:
    repo: deb [arch=amd64 signed-by=/etc/apt/trusted.gpg.d/hashicorp.asc] https://apt.releases.hashicorp.com bullseye main
  when: ansible_distribution == "Debian"

- name: add reposiroty for HashiCorp on Ubuntu
  become: true
  apt_repository:
    repo: deb [arch=amd64 signed-by=/etc/apt/trusted.gpg.d/hashicorp.asc] https://apt.releases.hashicorp.com {{ ansible_distribution_release }} main
  when: ansible_distribution == "Ubuntu"

- name: install HashiCorp tools
  become: true
  apt:
    pkg:
      - consul
      - nomad
      - vagrant
      - vault

- name: install Terraform
  become: true
  apt:
    pkg:
      - terraform
  when: private_node

- name: deactivate nomad service
  ansible.builtin.systemd:
    name: nomad
    state: stopped
    enabled: no
